httpx==0.27.2
rich==13.9.4
# optional — only needed to make the swarm LLM-commanded instead of offline:
anthropic==0.39.0 ; extra == "llm"
# pip install anthropic      # for ANTHROPIC_API_KEY
# pip install openai         # for OPENAI_API_KEY
